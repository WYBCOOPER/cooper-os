# REDESIGN_CHECKLIST.md — COOPER OS 重做避坑清单

> 2026-08-04 建立。回顾 8.2-8.3 重做时的所有问题，这次重做前/中/后逐条对照！
> 每完成一步，必须跑一遍本清单，全部 ✅ 才算过。

## 🔴 致命级（防功能崩溃）

- [ ] **浮层默认隐藏**：所有 modal/dropdown/panel 初始 `display:none`，`.open` 才显示
  - 上次坑：modal-overlay 默认 flex → 一进页面卡死
- [ ] **JS 语法检查**：任何修改后跑 `node --check`（提取全部 <script>）
  - 上次坑：禁用弹窗把 function 名替换坏 → 全 JS 崩溃 → 课程表消失
- [ ] **禁用功能保留函数名**：只改函数体，不删/不换函数名结构
- [ ] **残留清理**：大改后搜索旧类名/旧容器，确认无残留
  - 上次坑：旧 galaxy 容器没删 → "第二个大圆球"

## 🟠 严重级（防界面错乱）

- [ ] **顶栏不遮挡**：定位元素算容器边界（top/left 用 % 注意溢出）
- [ ] **transform 不冲突**：有 translate 定位的元素，hover/active 保留原 transform
  - 上次坑：translateX(-50%) 被 scale 覆盖 → 点击跳动
- [ ] **功能全覆盖**：列出全部功能清单，逐一确认入口存在
  - 上次坑：仪表盘只放 4 圆，日记/知识库/工具没了
- [ ] **手机端适配**：760px 断点检查，无横向滚动，底部导航正常

## 🟡 功能级（防体验问题）

- [ ] **核心同步绑事件**：同步内容不只是 innerHTML，要绑定 onclick（完成/删除）
- [ ] **数据按日期分桶**：今日类数据用 `key_日期` 结构，跨天自动重置
- [ ] **设计先对齐**：先看审美档案（DESIGN_SYSTEM.md）→ 概念图确认 → 再动手
  - 上次坑：金色→Linear→回滚，方向没对齐

## ⚪ 流程级（防操作坑）

- [ ] **脚本用文件**：不用 `node -e "..."` 内联（引号坑），一律 write 文件
- [ ] **端口确认**：测试/访问前确认目标端口（3000 vs 3001 vs 内嵌）
- [ ] **安全测试**：改完跑 security_test.js（正负路径）
- [ ] **回归测试**：跑 regression_test2.js（历史 bug 不复发）
- [ ] **版本留痕**：改动记 CHANGELOG + 版本号
- [ ] **备份先行**：大改前备份 index.html

## ✅ 验收标准
1. 打开无弹窗卡死
2. 课程表/待办/聚焦/日记全部功能可用
3. 无重叠、无遮挡、无跳动
4. 手机端正常
5. 回归测试全过
