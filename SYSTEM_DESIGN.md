# COOPER OS 系统设计文档 v2（2026-08-01）

> 本文档是 COOPER OS 的"宪法"。所有板块的使用逻辑、联动规则、数据模型都在此固定。
> 以后任何修改必须先过本文档，不允许破坏下面定义的规则。

---

## 一、核心设计哲学

**"掌控感 = 做减法 + 让数据自己流动"**

- 用户只做**最小动作**（打卡、勾选、输入一句话）
- 系统负责**自动流转**（联动、汇总、提醒、复盘）
- 每个数据**只有一个主人**，其他板块只读或引用，不重复存储

---

## 二、板块层级（按"从大到小"组织）

```
┌─────────────────────────────────────────────┐
│  🎯 项目（为什么做）—— 战略层                 │
│    大目标：COOPER OS / 学 C / 家教服务         │
├─────────────────────────────────────────────┤
│  📊 学习追踪（长期进度）—— 习惯层             │
│    持续数周：C 165集 / Python 30章 / 英语4500词│
├─────────────────────────────────────────────┤
│  🎯 今日聚焦（今天最重要的 3 件事）—— 战术层   │
│    "写程序" / "背 30 词" / "改简历"           │
├─────────────────────────────────────────────┤
│  ✅ 待办清单（所有要做的事）—— 任务池          │
│    零散任务：买数据线 / 回消息 / 拆解子问题    │
├─────────────────────────────────────────────┤
│  🚨 DDL（有截止日期的事）—— 时间层            │
│    交作业 08-05 / 项目截止 09-01              │
└─────────────────────────────────────────────┘
```

**一句话概括：**
> 项目是"为什么"，追踪是"长期进度"，聚焦是"今天做啥"，待办是"所有要做的事"，DDL 是"什么时候截止"。

---

## 三、各板块使用逻辑（谁负责什么）

| 板块 | 用户动作 | 系统自动做 | 数据主人 |
|------|---------|-----------|---------|
| 🎯 项目管理（新建） | 填名称/类型/DDL/联动选项 | 生成看板卡片、DDL 汇入总台账 | `cg_projects` |
| 📊 学习追踪 | 打卡 ➕（+步进）或 ✏️改 | 进度条、今日统计、复盘汇总 | `cg_track` |
| 🎯 今日聚焦 | 添加（可绑追踪）→ 勾选完成 | 绑定项自动打卡 | `cg_focus` |
| ✅ 待办清单 | 添加/勾选/删除 | 拆解子问题自动汇入 | `cg_todos` |
| 🚨 DDL 管理 | 添加/删除 | 到期自动转待办 | `cg_ddls` |
| 🌙 晚间复盘 | 确认/补充 | 预填今日聚焦+学习+待办+DDL | `cg_reviews` |

---

## 四、联动规则（固定！）

### 规则 1：聚焦 ↔ 追踪（手动绑定，不靠关键词）
- 聚焦项添加时可选择**绑定一个追踪项**（下拉：无/C/Python/LeetCode/英语/自定义）
- 聚焦勾选 ✅ → 绑定追踪项 **+步进**（英语 +30 词）
- 追踪打卡 ➕ → 绑定该追踪的聚焦项自动 ✅
- **未绑定 → 不联动**

### 规则 2：DDL → 待办（自动转）
- DDL 到期（今天 < 截止日）→ 自动生成待办"📌 xxx（已到期）"
- 从 DDL 列表移除，红色标记
- 每分钟检查一次

### 规则 3：项目 → 各层（可选联动，创建时勾选）
- 项目可勾选：☑ 同步 DDL ☑ 生成子任务→待办 ☑ 关联追踪 ☑ 关联拆解
- 项目 DDL 自动汇入 DDL 总台账（标注"来自项目"）
- 项目生成子任务 → 自动进待办

### 规则 4：拆解 → 待办
- 拆解画布的子问题点"→待办" → 进待办（🧩 标记）

### 规则 5：复盘汇总（晚间 21:00）
- 自动预填：今日聚焦（✅/⬜）+ 今日学习（打卡数）+ 完成待办 + 近3天 DDL
- 用户只需补充"为什么没完成"+"明天计划"

---

## 五、数据模型（store 键定义）

### `cg_track` —— 学习追踪（数组）
```js
[
  { key:'c', name:'📗 C 语言', cur: 6, goal: 165, unit:'集', step:1,
    link:'bilibili://video/BV1Xa4y1k7LU', kw:[], bind:[] },
  { key:'en', name:'📖 英语单词', cur: 320, goal: 4500, unit:'词', step:30,
    link:'', kw:[], bind:[] }
]
```
- `step`：每次打卡增加的量（英语 30，其他 1）
- 删除默认项后可用"恢复默认"找回

### `cg_focus` —— 今日聚焦（数组）
```js
[
  { t:'写程序', done:false, bind:'c' },   // bind = 绑定的追踪 key，null 表示不绑定
  { t:'背单词', done:true, bind:'en' }
]
```

### `cg_track_today` —— 每日打卡记录
```js
{ '2026-08-01': { c:2, en:1, lc:3 } }
```

### `cg_projects` —— 项目管理（新增）
```js
[
  { id:1, name:'COOPER OS', type:'软件', version:'10.1.0',
    status:'进行中', ddl:'2026-09-01', progress:80,
    links:{ ddl:true, todo:true, track:false, split:true } }
]
```
- `type`：软件/学习/生活/其他（软件类型显示版本号）
- `status`：🟢进行中 🟡暂停 ✅完成 ⚪放弃
- `links`：联动开关（创建时勾选）

### `cg_focus_linked` —— 防重复打卡记录
```js
{ '2026-08-01': { 0:true, 1:true } }   // 聚焦索引 → 已联动
```

---

## 六、界面布局（改动后）

```
📚 学业：
   🧩 问题拆解 | 📈 学习追踪
   📅 课程表   | 🚨 DDL 管理
💼 工作：
   ✅ 待办清单 | 📁 项目管理（新）
   📄 最近文档 | 🔌 软件管理
🏠 生活：
   💧 喝水 | 🌙 作息
   💊 用药 | ✦ 成长档案
   🎯 社团
🤖 对话 | 📓 日记 | 🧠 知识库 | 🌐 工具
```

---

## 七、本次修改清单（按此执行）

1. **学习追踪**：加 `step` 步进字段（英语 30，编辑时可改）
2. **今日聚焦**：添加时可选绑定追踪项（下拉）；勾选完成 → 绑定追踪 +step
3. **追踪打卡**：+step 而非 +1；打卡 → 绑定聚焦项自动 ✅
4. **项目管理**：新板块（工作区），看板视图 + 类型/版本号/DDL/联动勾选
5. **DDL 从属**：项目 DDL 自动汇入 DDL 板块（带"来自项目"标记）
6. **删除关键词联动**：不再用关键词猜，全部手动绑定
7. **复盘**：汇总时把项目进度也纳入

---

## 八、铁律（以后改代码前必读）

1. 数据只有一个主人，其他只读引用
2. 联动必须手动绑定，禁止关键词猜测
3. 所有 store 键变更必须在 CHANGELOG 记录
4. 版本号按 SemVer 递增（MAJOR.MINOR.PATCH）
5. 大改前先备份 index.html

## 九、用户特性清单（每次优化前必读）

> 优化方向必须兼顾宝宝特性，不能丢方向。核心哲学：掌控感 = 做减法。

| 特性 | 系统含义 |
|------|---------|
| 思维超越行动 | 系统把"想"自动变"做"：拆解→待办、项目→子任务 |
| 贪心、易被杂乱迷惑 | 一切功能服务做减法：板块宁少勿多 |
| 目标感强、争强好胜 | 要看得见的成果：打卡率图表、周回顾、进度条 |
| 作息紊乱（3-4点睡） | 作息记录 + 23:30 提醒，但不强迫 |
| 自驱型学习者 | 学习追踪是核心，C/Python 双线是主线 |
| 固执 | 功能全部可开关，不替用户决定 |

**优化铁问：这个功能是帮宝宝少做一件事，还是让他多做一件事？只做前者。**

## 十、发布流程（正式版本）

1. 更新 CHANGELOG.md + package.json 版本号
2. 打包 exe → dist\COOPER OS X.X.X.exe
3. 桌面覆盖固定入口 桌面\COOPER OS.exe（名字不变，指向最新版）
4. 删除旧版桌面入口 + 清理 dist 旧 exe（只留最新）
5. 同步网页版副本 桌面\草哥工作台.html
6. 启动验证（端口 3000 监听 + 进程存在）

## 十一、留痕原则（宝宝要求）

- 所有项目都要留痕：版本（SemVer）/ 日志（CHANGELOG）/ Git（commit）/ 日记（复盘）
- 项目阶段性成果 → 提醒 git commit
- 重要决策 → 写入本文档或 CHANGELOG
